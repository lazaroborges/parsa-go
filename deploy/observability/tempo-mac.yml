stream_over_http_enabled: true

server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: "0.0.0.0:4317"

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo-data/traces
    wal:
      path: /tmp/tempo-data/wal

metrics_generator:
  storage:
    path: /tmp/tempo-data/metrics
  traces_storage:
    path: /tmp/tempo-data/traces